version: '3'
services:
    api:
        build: ./api
        volumes:
            - ./api:/go/src/api
        ports:
            - 8090:8090
    web:
        build: ./web
        volumes:
            - ./web:/app
            - /app/node_modules
        ports:
            - 8080:8080
