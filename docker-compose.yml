version: '3'
services:
    api:
        build: ./api
        volumes:
            - ./api:/go/src/api
        ports:
            - 8090:8090
    web:
        build: ./web
        volumes:
            - ./web:/app
        ports:
            - 8080:8080